# Зависимости аудио-слоя JARVIS-OS (audio_layer.py: ASR + TTS).
# Базовый образ — vLLM, в нём УЖЕ есть torch/CUDA/numpy/fastapi/uvicorn, поэтому
# тут только аудио-специфика и без жёстких пинов на то, что уже стоит (иначе pip
# начнёт переустанавливать torch/numpy и качать их заново).
fastapi
uvicorn[standard]
faster-whisper==1.1.0
webrtcvad==2.0.10
numpy<2
kokoro==0.3.4
soundfile==0.12.1
